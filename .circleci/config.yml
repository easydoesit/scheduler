version: 2
jobs:
  build:
    docker:
      - image: cimg/node:19.7

    working_directory: ~/client

    steps:
      - run: echo "deploy build run"

  deploy:
    docker:
      - image: cimg/node:19.7

    steps:
      - run: echo "deploy workflow run"

  workflows:
    version: 2
    test_and_deploy:
      jobs:
        - build:
        - deploy:
            requires:
              - build
